# Plan (implemented in this branch)

See `/home/roshchupkin/GENNET_IMPROVE_LAB_PLAN.md` on this workstation and `improve/README.md`.

What landed:

1. `improve/` lab: schema, roles, seeded backlog, `run.py`
2. Hardened tests: import, convert, interpret; one ArgparseSimulator; CLI path from `__file__`
3. OpenAI-compatible `propose` / `one-task` with `--dry-run`
4. Smoke sim entries and `improve/sims/planted_pathway.py`
